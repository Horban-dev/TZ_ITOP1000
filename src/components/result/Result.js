import React from 'react';

const Result = ({result}) => {
    return (
        <div >
            <h1 style={{textAlign: 'center'}}>Result: {result}</h1>
        </div>
    );
};

export default Result;